projection implementation in class ZBP_C_PSBTP_ATRAV unique;
strict ( 2 );
use draft;
define behavior for ZC_PSBTP_ATRAV alias Travel
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action deductDiscount;
  use action acceptTravel;
  use action rejectTravel;
  use action copyTravel;
}